rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEVELOPMENT RULES - ALLOW ALL AUTHENTICATED USERS
    // ⚠️ REPLACE WITH SECURE RULES IN PRODUCTION ⚠️
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}